# AUTO-GENERATED by scripts/make_ablation_configs.py — do not hand-edit.
# single-task anomaly detector: only the decoder/reconstruction head is trained
# Diff vs configs/default.yaml: train.loss_weights, paths.*

project: cluster-heartbeat
seed: 42
data:
  source: synthetic
  synthetic:
    nodes: 16
    steps: 720
    interval_seconds: 60
    out_dir: data/synthetic
    incident_probability: 0.5
    seed: 42
  prometheus:
    url: http://localhost:9090
    lookback_hours: 12
    step_seconds: 60
    timeout_seconds: 30
    verify_tls: false
  alibaba:
    raw_dir: data/alibaba
    machine_usage_file: machine_usage.csv
    max_machines: 50
window:
  size: 30
  stride: 5
  forecast_horizon_steps: 1
  ttf_horizon_hours: 12
model:
  type: autoencoder
  latent_dim: 48
  hidden_dims:
  - 512
  - 256
  dropout: 0.1
  use_deltas: true
  pca_components: 16
train:
  epochs: 30
  batch_size: 64
  lr: 0.001
  weight_decay: 1.0e-05
  val_split: 0.25
  patience: 6
  device: auto
  mask_prob: 0.1
  loss_weights:
    reconstruction: 1.0
    classification: 0.0
    demand: 0.0
    ttf: 0.0
scoring:
  temperature:
    warn_celsius: 80
    crit_celsius: 90
  anomaly:
    z_full_scale: 6.0
    alert_threshold: 0.7
  failure:
    risk_alert_threshold: 0.6
  idle:
    gpu_util_max: 5.0
    mem_util_max: 10.0
    min_consecutive_windows: 3
  cost:
    electricity_usd_per_kwh: 0.12
    gpu_hourly_cost_usd: 2.5
paths:
  checkpoints: checkpoints/ablation_reconstruction_only
  logs: logs
  reports: reports/ablation_reconstruction_only
